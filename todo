add vim mode to textareas
add routines
add a journal page
make sure the Day page updates after x mins
adding a page with the same name overwrites the existing page
clean up the package.json/docker mess
