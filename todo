make data stay across page change

setup the build properly, have the server bundled and something that runs off the dist/ content
will change to the wrong color when stopped in recess????
add comments/edits to saved clips/timestamps
add an event listener for space bar to start stop timer, plus an enter one to start initially
unpause takes to long to restart
